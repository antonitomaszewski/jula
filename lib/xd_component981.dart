import 'package:flutter/material.dart';

class XDComponent981 extends StatelessWidget {
  XDComponent981({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Container(),
      ],
    );
  }
}
